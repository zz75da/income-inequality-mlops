streamlit==1.36.0
pandas==2.2.2
plotly==5.22.0
requests==2.32.3
pyyaml==6.0.1
dvc[s3]==3.51.2  # only exercised in the cold-start hosted-deploy bootstrap (see app_streamlit.py)
pathspec==0.11.2  # pin explicitly — some other pinned dep's loose constraint otherwise pulls
                   # pathspec 1.1.1, which is missing _DIR_MARK and breaks every dvc command
