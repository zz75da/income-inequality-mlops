name: CI — Tests & DVC Sync

on:
  push:
    branches: [main, master, "experiment/**"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  tests:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest

    permissions:
      checks: write
      contents: read

    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install service dependencies
        run: |
          python -m pip install --upgrade pip
          pip install \
            "fastapi==0.111.0" \
            "uvicorn[standard]==0.30.1" \
            "pydantic==2.7.4" \
            "requests==2.32.3" \
            "httpx>=0.27.0" \
            "prometheus-client==0.21.0" \
            "prometheus-fastapi-instrumentator==6.1.0" \
            "pandas==2.2.2" \
            "numpy==1.26.4" \
            "scikit-learn==1.4.2" \
            "xgboost==2.0.3" \
            "pyyaml==6.0.1" \
            "pycountry==24.6.1"

      - name: Install test dependencies
        run: pip install -r tests/requirements-test.txt

      - name: Run unit tests
        run: pytest tests/unit/ -v --tb=short -q --junitxml=tests/logs/unit-results.xml

      - name: Run integration tests
        run: pytest tests/integration/ -v --tb=short -q -m integration --junitxml=tests/logs/integration-results.xml

      - name: Publish test results
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Pytest Results
          path: tests/logs/*-results.xml
          reporter: java-junit
          fail-on-error: false

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs
          path: tests/logs/
          if-no-files-found: ignore

  dvc-sync:
    name: DVC Status Check
    runs-on: ubuntu-latest
    needs: tests
    if: github.event_name == 'push'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install DVC
        run: pip install "dvc[s3]"

      - name: DVC pull (verify remote is reachable and in sync)
        env:
          DAGSHUB_USER: ${{ secrets.DAGSHUB_USER }}
          DAGSHUB_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          dvc remote modify origin --local access_key_id "$DAGSHUB_TOKEN"
          dvc remote modify origin --local secret_access_key "$DAGSHUB_TOKEN"
          dvc status --cloud || echo "DVC remote not configured yet — skipping (set DAGSHUB_USER/DAGSHUB_TOKEN repo secrets to enable)"
